{
  "name": "vault-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/background.ts src/content-main.ts src/content-bridge.ts src/sidepanel.ts --bundle --outdir=dist --format=iife",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
